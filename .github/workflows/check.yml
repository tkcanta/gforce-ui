name: Design contract and browser checks
on: [push, pull_request]
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run check
      - run: git diff --exit-code -- assets examples/files.html
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: files-review-evidence
          path: test-results/files/
